## US_Visa_Approval_Prediction


## Workflow
update config.yaml
update schema.yaml
update params.yaml
update the entity
update the configuration manager in src config
update the components
update the pipeline
update the main.py
update the app.py

## How to Start or Run

```
conda create -p visa python==3.9 -y
```

```
conda activate visa/
```

```
pip intall -r requirements.txt
```

```
git config -- global user.email "raguramanraja45@gmail.com"
```

```
git config --global user.name "Raguramrrr"
```

#### first we create folder structure

```
template.py
```

next we setup our project by ``` setup.py ```, then we have run some
requires libraries ``` requirements.txt ```